# SQLAlchemy and PostgreSQL
sqlalchemy>=2.0.0
psycopg2-binary>=2.9.0

# pgvector support for PostgreSQL
pgvector>=0.3.0

# Optional: async support (if using async SQLAlchemy)
# asyncpg>=0.29.0

# Marker document parser (PDF / 이미지 / Office / HTML 등)
marker-pdf

# HTML → PDF 변환 (Marker HTMLProvider 의존성)
WeasyPrint>=62.0

# Chunking
langchain-text-splitters
tiktoken

# Embedding
sentence-transformers

# Cloudflare R2(S3 호환) 클라이언트
boto3

# HTTP 클라이언트 (Datalab Marker API 호출용)
requests

# API (FastAPI 사이드카 서버)
fastapi>=0.115.0
uvicorn[standard]>=0.32.0
python-multipart
